# Context
Please review the `PROJECT_SUMMARY.md` file to understand the current architecture of the Urban Intelligence Platform (UIP). We are currently expanding **SUB-02: Agent Behavior**. 

The current system successfully handles mode choice via MNL, agent memory/frustration, household dynamics, and multi-leg schedules using Python >=3.11, `numpy`, and `pandas`.

# Task
I need to add new economic and retail behaviors to the simulation. Specifically, we need to model the interactions between formal retail (shops), informal retail (roadside stalls), and the citizens (shoppers). 

Please generate the Python classes and behavioral logic for the following new agent roles. Ensure they inherit from or integrate cleanly with our existing base Agent classes, memory system, and scheduling system.

## 1. Informal Retail Agents (Roadside Stalls)
Create a base `StallOwner` class with specific subclasses: `FoodStallOwner`, `ClothesStallOwner`, and `AccessoriesStallOwner`.
*   **Behavioral Logic:** 
    *   **Dynamic Location Choice:** Unlike formal stores, stall owners should evaluate foot traffic and spatial utility. If sales/foot traffic are low, their "frustration" memory should increase, prompting them to move to a new node/location.
    *   **Dynamic Pricing & Inventory:** Food stall owners should have faster inventory decay (perishables) compared to clothes stall owners.
    *   **Vulnerability:** Add a small probability of "disruption" (e.g., weather, police clearance) that heavily impacts their daily schedule and frustration metrics.

## 2. Formal Retail Agents (Stores)
Create classes for `StoreManager` and `StoreStaff`.
*   **Store Manager Behavior:**
    *   Makes decisions on pricing and inventory restocking based on a weekly schedule.
    *   Assigns working shifts to `StoreStaff`.
*   **Store Staff Behavior:**
    *   Their daily multi-leg schedule (Home -> Work -> Home) is strictly dictated by the shifts assigned by the `StoreManager`.
    *   Their "frustration" should increase if commute delays cause them to be late for their shift.

## 3. Citizen Extension: The Shopper
Extend our existing citizen agent logic to include "Shopping" as a valid leg in their multi-leg journey planning.
*   **Mode Choice & Destination Choice:** Use MNL utility functions to help the agent decide *where* to shop (Formal Store vs. Roadside Stall) based on attributes like:
    *   Income level (from household dynamics).
    *   Distance/Travel Time.
    *   Product type needed (e.g., quick food vs. clothes).

# Output Requirements
1.  **Code:** Provide the Python code for these classes, complete with type hinting (`mypy` compliant) and docstrings.
2.  **Integration:** Show a brief example of how a `Shopper` agent interacts with a `FoodStallOwner` agent, updating the memory/frustration of both (e.g., shopper successfully buys food -> utility increases; stall owner makes a sale -> frustration decreases).
3.  **Testing:** Provide a quick `pytest` snippet to validate that a `StallOwner` relocates when their frustration exceeds a certain threshold.